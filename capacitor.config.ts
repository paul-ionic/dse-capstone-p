import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.lacaille.dsecapstonepaul',
  appName: 'dse_capstone_p',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
