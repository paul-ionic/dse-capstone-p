import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dse_capstone_p',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
